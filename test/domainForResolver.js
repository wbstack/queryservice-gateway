const assert = require('assert');
var r = require("./../resolver.js")

describe('resolver', function() {
    it('null test', function() {
        assert.strictEqual(
            null,
            null
        );
    });
});
